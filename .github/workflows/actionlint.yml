name: ActionLint

on:
  push:
    paths:
      - .github/workflows/** 
  workflow_dispatch:

jobs:
  call-actionlint-workflow:
    uses: svekar/reusable-workflows/.github/workflows/actionlint.yaml@d7c29a092101c4f15b35cf7c77664db86116d441 # main 
